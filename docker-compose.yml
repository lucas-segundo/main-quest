version: '3.5'

services:
  db:
    image: postgres
    restart: always
    environment:
      - POSTGRES_PASSWORD=postgres
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  app:
    env_file:
      - .env
    command: ${CMD_LINE}
    build:
      context: .
      target: development
    ports:
      - '3000:3000'
      - '9229:9229'
    depends_on:
      - db
    volumes:
      - .:/app
