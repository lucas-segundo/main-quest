name: CI/CD

env:
  DOCKER_IMAGE_NAME: main-quest:${{ github.ref_name }}

on:
  push:
    branches: [main]

jobs:
  ci:
    environment: ${{ github.ref_name }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build dev docker image
        run: docker compose build dev
        env:
          DOCKER_IMAGE_NAME: ${{ env.DOCKER_IMAGE_NAME }}
          POSTGRES_DATABASE_URL: ${{ vars.DOCKER_POSTGRES_DATABASE_URL }}

      - name: Run unit tests
        run: docker run ${{ env.DOCKER_IMAGE_NAME }} npm run test

      - name: Run e2e tests
        run: docker run ${{ env.DOCKER_IMAGE_NAME }} npm run test:e2e

      - name: Run migrations for testing
        run: docker run ${{ env.DOCKER_IMAGE_NAME }} npm run migrate:prod

      - name: Run migrations for production
        run: docker run -e POSTGRES_DATABASE_URL=${{ secrets.POSTGRES_DATABASE_URL }} ${{ env.DOCKER_IMAGE_NAME }} npm run migrate:prod

  cd:
    needs: ci
    environment: ${{ github.ref_name }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Prod Docker image and push to DockerHub
        uses: docker/build-push-action@v5
        with:
          target: prod
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/${{ env.DOCKER_IMAGE_NAME }}
